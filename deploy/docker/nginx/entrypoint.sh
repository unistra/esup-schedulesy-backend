#!/bin/sh

# TODO: reuse later !!!

# if [ -n "$UWSGI_URI" ]; then
# 	sed -e "s/{{UWSGI_URI}}/$UWSGI_URI/" config.conf > /etc/nginx/nginx.conf
# else
#   sed -e "s/{{UWSGI_URI}}/0.0.0.0:8080/" config.conf > /etc/nginx/nginx.conf
# fi

# exec "$@"